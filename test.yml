jobs:
  - deployment: job1
    environment: Env1.VM1
    strategy:
        runOnce:
          deploy:
            steps:
              - script: echo 'hello world1'
                displayName: 'Run a one-line script'
