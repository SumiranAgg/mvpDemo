jobs:
  - deployment: job1
    environment: Env1.default-default
    strategy:
        runOnce:
          deploy:
            steps:
              - script: echo 'hello world1'
                displayName: 'Run a one-line script'
